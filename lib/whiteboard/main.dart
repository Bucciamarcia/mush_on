import 'package:flutter/material.dart';

class GeneralWhiteboard extends StatelessWidget {
  const GeneralWhiteboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
