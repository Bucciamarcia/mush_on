import 'package:flutter/material.dart';

class EmailRemindersSettings extends StatelessWidget {
  const EmailRemindersSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
